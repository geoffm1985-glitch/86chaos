#!/usr/bin/env node
'use strict';const fs=require('node:fs'),path=require('node:path'),assert=require('node:assert/strict'),root=path.resolve(__dirname,'..'),read=f=>fs.readFileSync(path.join(root,f),'utf8'),json=f=>JSON.parse(read(f));assert.equal(json('package.json').version,'17.1.27');assert.equal(json('package-lock.json').version,'17.1.27');assert.equal(json('public/version.json').version,'17.1.27');assert(read('src/core/localReminderBridge.js').includes('LocalNotifications'));assert(!/firebase|firestore|secureFetch|\/api\//i.test(read('src/core/localReminderBridge.js')));assert(read('src/features/intelligence.jsx').includes('This device only'));assert(!read('src/features/intelligence.jsx').includes('Share with teammate'));assert(read('scripts/86chaos-release-gate/release-test-universe.cjs').includes('44-device-local-reminders-experimental.spec.cjs'));console.log('17.1.27 reminder validation passed.');
