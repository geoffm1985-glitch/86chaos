// Compatibility shim: the live Recipes screen is src/features/operations.jsx::TabRecipes.
// Keep this file from drifting into a second, unused implementation.
export { TabRecipes as default } from '../features/operations';
