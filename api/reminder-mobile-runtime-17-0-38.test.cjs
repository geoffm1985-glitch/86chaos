'use strict';
const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path');
const root=path.resolve(__dirname,'..'),read=f=>fs.readFileSync(path.join(root,f),'utf8');
test('17.0.38 normalizes persisted device-local reminder values before React render',()=>{const bridge=read('src/core/localReminderBridge.js'),ui=read('src/features/intelligence.jsx');assert.match(bridge,/normalizeDeviceLocalReminder/);assert.match(bridge,/x\.map\(normalizeDeviceLocalReminder\)\.filter\(Boolean\)/);assert.match(ui,/normalizeReminderUiRow/);assert.match(ui,/data-reminder-runtime-safety="17\.0\.38"/)});
test('17.0.38 full-gate marker is explicit while normal testing pushes remain targeted delta',()=>{const workflow=read('.github/workflows/testing-targeted-delta.yml');assert.match(workflow,/\[full-gate\]/);assert.match(workflow,/Run 17\.0\.38 targeted delta only/);assert.match(workflow,/Run full Release Gate/);assert.match(workflow,/read-crash-report-for-ci\.cjs/)});
test('17.0.38 adds real mobile corrupted-storage regression coverage',()=>{const spec=read('tests/86chaos-release-gate/47-reminder-mobile-runtime-recovery.spec.cjs');assert.match(spec,/deviceLocalReminders:v1/);assert.match(spec,/legacy-object-title/);assert.match(spec,/86 Chaos App Recovery/i);assert.match(spec,/mobile reminder surface survives malformed persisted rows/i)});
