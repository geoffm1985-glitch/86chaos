'use strict';const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path');const read=p=>fs.readFileSync(path.join(__dirname,'..',p),'utf8');test('experimental device-local reminder bridge is zero Firebase',()=>{const s=read('src/core/localReminderBridge.js');assert.match(s,/LocalNotifications/);assert.match(s,/navigator\.share/);assert.doesNotMatch(s,/firebase|firestore|secureFetch|\/api\//i)});test('experimental reminder UI removes teammate sharing',()=>{const s=read('src/features/intelligence.jsx');assert.match(s,/This device only/);assert.doesNotMatch(s,/Share with teammate/)})