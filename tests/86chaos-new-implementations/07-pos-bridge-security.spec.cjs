'use strict';const {test,expect}=require('@playwright/test');
test('POS Bridge rejects human, Shift4 and malformed bearer tokens',async({request})=>{for(const token of ['firebase-human-token','shift4-provider-token','not.a.jwt']){const response=await request.get('/api/pos-bridge/v1/capabilities',{headers:{'X-POS-Bridge-Contract':'1.0',Authorization:`Bearer ${token}`}});expect([401,503]).toContain(response.status());const body=await response.json();expect(body.ok).toBe(false);}});
test('POS Bridge rejects client-supplied authority fields before staging',async({request})=>{const response=await request.post('/api/pos-bridge/v1/events',{headers:{'X-POS-Bridge-Contract':'1.0',Authorization:'Bearer not.a.jwt'},data:{restaurantId:'forged',locationId:'foreign',events:[{status:'accepted',processingStatus:'processed'}]}});expect(response.status()).toBe(403);const body=await response.json();expect(body.ok).toBe(false);expect(body.code).toBe('forbidden');});
