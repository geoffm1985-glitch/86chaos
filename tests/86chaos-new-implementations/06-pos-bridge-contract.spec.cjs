'use strict';const {test,expect}=require('@playwright/test');
test('POS Bridge v1 rejects unsupported contracts before any machine write',async({request})=>{for(const route of ['token','events','reconciliation']){const response=await request.post(`/api/pos-bridge/v1/${route}`,{data:{}});expect(response.status(),route).toBe(426);const body=await response.json();expect(body.code).toBe('unsupported_contract');}});
test('POS Bridge capabilities is machine-authenticated and never advertises an outbox',async({request})=>{const response=await request.get('/api/pos-bridge/v1/capabilities',{headers:{'X-POS-Bridge-Contract':'1.0'}});expect([401,503]).toContain(response.status());const text=await response.text();expect(text).not.toContain('outbox');expect(text).not.toContain('staff.read');expect(text).not.toContain('menu.read');});
