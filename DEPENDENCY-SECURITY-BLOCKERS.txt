SECURITY_REPAIR_COMPLETE = pending-network-audit-validation

16.0.178 surgical dependency repair:
- Firebase Web SDK remains pinned to 11.10.0.
- Firebase Admin SDK remains pinned to 13.10.0.
- @firebase/rules-unit-testing is pinned to 4.0.1, whose Firebase 11.10.0 source declares peer firebase ^11.0.0.
- firebase-tools remains 15.24.0; no --force downgrade/upgrade was used.
- Vite/Jest migration from 16.0.177 is preserved.
- Stale unreachable CRA-era package-lock entries were removed; these were the source of the reported HIGH nth-check/postcss/serialize-javascript chains.

Network validation required before marking true:
  npm install --include=dev --no-audit --no-fund
  npm audit --audit-level=high

Do not mark this true unless the audit exits 0 with HIGH=0 and CRITICAL=0.
